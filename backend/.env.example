GROQ_API_KEY=your _groq_api_key_here
GROQ_MODEL=openai/gpt-oss-120b
# Optional - how many papers the Literature Agent pulls per query (default 20).
# Higher = broader coverage but slower and uses more Groq quota (one LLM
# call per paper in the Analysis Agent).
MAX_PAPERS=20
